# Browser API base (use local dev proxy to avoid CORS)
VITE_API_BASE=/api

# Upstream backend target for dev proxy / server proxy (local-first)
API_PROXY_TARGET=http://localhost:8080
#API_PROXY_TARGET=https://pickleball-backend-production-150f.up.railway.app
